# Backend (Render) environment example
# NOTE: Do not set PORT for local dev; the app defaults to 3000.
# Render injects PORT automatically in production.
# PORT=10000

# Allow your Vercel frontend domain(s), comma-separated (no trailing slash)
# Example after deploy: https://your-vercel-app.vercel.app
CORS_ORIGIN=https://your-vercel-app.vercel.app,http://localhost:5173

# SMTP configuration (use your provider or Gmail App Password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=shaiksameer3909sam@gmail.com
SMTP_PASS=dvnutpiashupjrqb

# Recipient email (defaults to SMTP_USER if not set)
TO_EMAIL=shaiksameer3909sam@gmail.com
